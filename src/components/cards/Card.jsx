import styled from 'styled-components';

const Card = styled.section`
  minheight: 100vh;
  display: grid;
  margin-top: 0em;
  margin-bottom: 0em;
  margin-left: 5vw;
  margin-right: 5vw;
`;

Card.displayName = 'Card';

export default Card;
