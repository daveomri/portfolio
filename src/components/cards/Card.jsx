import styled from 'styled-components';

const Card = styled.section`
    height: 100vh;
    max-height: 1000px;
    display: grid;
    padding-left: 5vw;
    padding-right: 5vw;
`;

Card.displayName = 'Card';

export default Card;
