import styled from 'styled-components';

const Card = styled.section`
  minheight: 100vh;
  display: grid;
  padding-top: 2em;
  padding-bottom: 2em;
  padding-left: 5vw;
  padding-right: 5vw;
`;

Card.displayName = 'Card';

export default Card;
